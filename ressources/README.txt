Placer ici les ressources graphiques de l'affiche :

  logo.png       — Logo du club (fond transparent recommandé, toute taille)
  fond.png       — Image de fond (optionnelle, toute taille)

Le logo sera automatiquement redimensionné pour tenir dans le header (max 180×84 px).
L'image de fond sera étirée sur toute la surface à faible opacité (fond transparent).
